import { createSlice } from '@reduxjs/toolkit';

export const topExposureSlice = createSlice({
  name: 'topExposure',
  initialState: '대표메뉴',
  reducers: {},
});

export default topExposureSlice.reducer;
